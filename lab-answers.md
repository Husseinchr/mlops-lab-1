# Lab 1 Answers — Git, DVC, and Data Preparation

## Question 1

Running `uv init` created the Python project skeleton while preserving the existing `README.md`:

- `.python-version` pins Python 3.12 for consistent local environments.
- `pyproject.toml` is the central project configuration. It contains the package metadata, required Python version, dependencies, build system, and console-script entry point.
- `src/mlops_lab_1/__init__.py` creates the importable Python package and initially contains a small example `main()` function.

After adding Pillow, `pyproject.toml` also records it as a dependency and `uv.lock` records the exact resolved dependency versions. The lock file makes installations reproducible and should be committed to Git.

## Question 2

`dvc init` created the following DVC repository files:

- `.dvc/config`: the repository-level DVC configuration. It is initially empty and later stores non-secret settings such as the remote name and URL.
- `.dvc/.gitignore`: prevents DVC's local cache, temporary files, and `config.local` from being tracked by Git.
- `.dvcignore`: lets DVC skip paths that are irrelevant to data tracking, similarly to how `.gitignore` works for Git.
- `.dvc/tmp/`: runtime state used internally by DVC. It is local and ignored by Git.

The files that describe the shared project configuration—`.dvc/config`, `.dvc/.gitignore`, and `.dvcignore`—should be pushed to Git. DVC cache files, temporary files, and `.dvc/config.local` should not be pushed.

## Question 3

Because the authentication commands used `--local`, the username, authentication method, and password are stored in `.dvc/config.local`. DVC automatically ignores this file through `.dvc/.gitignore`.

DVC configuration has four useful scopes:

- No scope option: repository scope, stored in `.dvc/config` and normally committed.
- `--local`: repository-specific private settings in `.dvc/config.local`.
- `--global`: settings for the current operating-system user, outside the repository.
- `--system`: machine-wide settings for all users.

Credentials must not be pushed to GitHub. Only the non-secret remote URL and default-remote setting belong in `.dvc/config`. A fresh clone therefore needs its own credentials supplied locally or through an equivalent secure mechanism.

## Question 4

After `dvc add data`, DVC added `/data` to the repository's `.gitignore`. This prevents Git from storing the image files directly. DVC copied the content into its content-addressed local cache and generated a small Git-trackable pointer file instead. This division keeps source code and metadata in Git while DVC versions the large dataset.

The Python-specific entries `.venv/`, `__pycache__/`, and `*.py[cod]` were also added so local environments and generated bytecode do not pollute the repository.

## Question 5

Yes, DVC created `data.dvc`. For the raw-data snapshot it contains one output entry with:

- the tracked path: `data`;
- a directory content hash: `a3a457d03c51ff8b037a833440f6ad13.dir`;
- the hash algorithm: MD5;
- the total size: 1,188,442,712 bytes;
- the total file count: 16,643.

The hash identifies the exact dataset version in the DVC cache and remote storage. The pointer does not contain the images or credentials.

## Question 6

The GitHub `main` branch contains the Python code, DVC configuration, `.gitignore`, and `data.dvc`. It does not contain the `data` directory because Git ignores that path. The two relevant references are:

- `.dvc/config`, which identifies the DVC remote storage URL;
- `data.dvc`, which identifies the exact dataset content by its hash.

After `dvc push`, the actual data objects are stored in DagsHub. DagsHub can associate those objects with the Git-tracked DVC pointer and display the versioned dataset, while GitHub continues to store only the lightweight metadata.

## Question 7

A clean Git clone contains `data.dvc` but no materialized `data` directory. The required command is:

```bash
dvc pull
```

This reads the remote definition from `.dvc/config`, downloads the objects identified by `data.dvc`, and reconstructs the `data` directory. Since credentials are intentionally not committed, they must first be configured securely for the new clone when the remote requires authentication.

## Data preparation

The preparation program is `src/food11/data.py`. It follows the class-directory layout expected by [`torchvision.datasets.ImageFolder`](https://docs.pytorch.org/vision/stable/generated/torchvision.datasets.ImageFolder.html), which is commonly used with ResNet image-classification workflows.

The script performs the following deterministic steps:

1. Reads each image label from the numeric prefix in its filename.
2. Maps labels `0` through `10` to the eleven Food-11 class names.
3. Resizes every image to 128×128 pixels using high-quality Lanczos resampling.
4. Writes the complete class-folder dataset to `data/food11_processed`.
5. Writes at most 100 images per class per split to `data/food11_processed_mini`.

The resulting counts are:

| Split | Raw | Processed | Mini |
|---|---:|---:|---:|
| Training | 9,866 | 9,866 | 1,100 |
| Evaluation | 3,347 | 3,347 | 1,096 |
| Validation | 3,430 | 3,430 | 1,096 |
| **Total** | **16,643** | **16,643** | **3,292** |

Every processed image was checked to have dimensions of exactly 128×128, and every mini class was checked to contain no more than 100 images.

## Question 8

After checking out the earlier commit containing the raw-only `data.dvc` and running `dvc checkout`, the generated `food11_processed` and `food11_processed_mini` folders are no longer present. The raw folder remains because it belongs to that older dataset snapshot.

After switching back to `main` and running `dvc checkout` again, DVC restores the complete current snapshot, including both processed folders. This demonstrates that Git selects the version of the DVC pointer, while DVC materializes the matching version of the data.
